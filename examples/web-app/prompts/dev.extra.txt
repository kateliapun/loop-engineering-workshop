=== context_files ===
- %%project_path%%/docs/ — deeper docs, when CLAUDE.md points you there.

=== lifecycle_notes ===

=== queue_filters ===

=== branch_setup ===
cd %%project_path%% && git checkout main && git pull --ff-only
git checkout -b task/<n>-<short-slug>

=== plan_notes ===
Follow the existing patterns for routes, data access, and components — CLAUDE.md names
them. New UI reuses the design-system components; a new component needs a reason in
the design note.

=== style_rules ===
Commit after each coherent step: `git commit -m "<type>(<area>): <what> (Ref #<n>)"`.

=== regression_check ===
Run the end-to-end regression suite against staging: `npm run test:e2e`. Add a spec for
the flow this task shipped if it's worth protecting going forward.

=== ship_deploy ===
git push -u origin HEAD
gh pr create --repo %%repo%% --base main --fill --body "Ref #<n>

<summary, tests, reviewer verdict>"
gh pr merge --repo %%repo%% --squash --delete-branch
%%deploy_cmd%%
Wait for the deploy, then open %%app_url%% and confirm the change is live before the Done comment.

=== extra_done_fields ===
- Deployed: %%app_url%% at <commit sha>
